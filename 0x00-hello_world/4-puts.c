#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 * Uses puts instead of printf
 **/
int main(void)
{
	char puzzle[] = "\"Programming is like building a multilingual puzzle";

	puts(puzzle);
	return (0);
}
